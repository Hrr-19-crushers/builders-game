import {expect} from 'chai';

import {botWelcome} from '../../src/client/utils/chatBot';

describe('chatbot', () => {
  it('can send a welcome message', ()=> {
    //TODO: how to mock the store?
  })
});